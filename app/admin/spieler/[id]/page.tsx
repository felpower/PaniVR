import Link from 'next/link';import{ArrowLeft}from'lucide-react';import{Query}from'node-appwrite';import{requireAdmin}from'@/lib/admin-auth';import{getTablesDB,getUsers,databaseId,playerScoresTableId}from'@/lib/appwrite-server';
export const dynamic='force-dynamic';
export default async function PlayerDetail({params}:{params:Promise<{id:string}>}){await requireAdmin();const{id}=await params;const user=await getUsers().get(id);const rows=(await getTablesDB().listRows({databaseId,tableId:playerScoresTableId,queries:[Query.equal('playerId',[id]),Query.orderDesc('playedAt'),Query.limit(100)],total:false,ttl:0})).rows;const sum=rows.reduce((a:any,r:any)=>({games:a.games+1,kills:a.kills+Number(r.kills||0),deaths:a.deaths+Number(r.deaths||0),headshots:a.headshots+Number(r.headshots||0),score:a.score+Number(r.score||0)}),{games:0,kills:0,deaths:0,headshots:0,score:0});return <main className="admin-page"><div className="admin-content"><div className="admin-title"><div><p className="admin-kicker">Spielerprofil</p><h1>{user.name||'Ohne Namen'}</h1><p>{user.email}</p></div><Link href="/admin/spieler"><ArrowLeft size={16}/> Alle Spieler</Link></div><section className="admin-stats"><article><span>Spiele</span><strong>{sum.games}</strong></article><article><span>Punkte</span><strong>{sum.score}</strong></article><article><span>KDA</span><strong>{((sum.kills)/Math.max(1,sum.deaths)).toFixed(2)}</strong></article></section><section className="admin-panel"><div className="admin-panel-head"><h2>Vergangene Spiele</h2></div>{rows.length?<div className="player-admin-list">{rows.map((r:any)=><div key={r.$id}><strong>{r.game||'Match'}</strong><span>{r.kills||0}/{r.deaths||0}/{r.assists||0} K/D/A · {r.headshots||0} Headshots · {r.score||0} Punkte</span><small>{r.playedAt?new Date(r.playedAt).toLocaleDateString('de-AT'):''}</small></div>)}</div>:<p className="admin-hint">Noch keine Spiele gespeichert.</p>}</section></div></main>}
