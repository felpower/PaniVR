import { NextRequest, NextResponse } from 'next/server';
import { ID } from 'node-appwrite';
import { contactTableId, databaseId, getTablesDB } from '@/lib/appwrite-server';
export const runtime='nodejs';
export async function POST(request:NextRequest){const body=await request.json().catch(()=>null) as Record<string,unknown>|null;if(!body)return NextResponse.json({message:'Ungültige Anfrage.'},{status:400});const name=String(body.name||'').trim(),email=String(body.email||'').trim().toLowerCase(),phone=String(body.phone||'').trim(),subject=String(body.subject||'Allgemeine Anfrage').trim(),message=String(body.message||'').trim();if(name.length<2||name.length>100||!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)||message.length<5||message.length>2000||body.consent!==true&&body.consent!=='on')return NextResponse.json({message:'Bitte fülle alle Pflichtfelder korrekt aus.'},{status:400});try{await getTablesDB().createRow({databaseId,tableId:contactTableId,rowId:ID.unique(),data:{name,email,phone,subject,message,status:'new',createdAt:new Date().toISOString()}});return NextResponse.json({ok:true},{status:201});}catch{return NextResponse.json({message:'Kontaktanfragen sind noch nicht eingerichtet.'},{status:503});}}
