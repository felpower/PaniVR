import { NextResponse } from 'next/server';import{requireAdmin}from'@/lib/admin-auth';import{deletePlayerData,getUsers}from'@/lib/appwrite-server';
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{await requireAdmin();const{id}=await params;await deletePlayerData(id);await getUsers().delete(id);return NextResponse.redirect(new URL('/admin/spieler',request.url))}catch{return NextResponse.json({error:'Spieler konnte nicht gelöscht werden.'},{status:400})}}
