import { NextResponse } from 'next/server';import{requireAdmin}from'@/lib/admin-auth';import{getUsers}from'@/lib/appwrite-server';
export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){try{await requireAdmin();const{id}=await params;await getUsers().delete(id);return NextResponse.redirect(new URL('/admin/spieler',process.env.NEXT_PUBLIC_SITE_URL||'http://localhost:3000'))}catch{return NextResponse.json({error:'Spieler konnte nicht gelöscht werden.'},{status:400})}}
