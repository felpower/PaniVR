import { NextResponse } from 'next/server';import{adminWriteGuard}from'@/lib/admin-auth';import{deletePlayerData,getUsers}from'@/lib/appwrite-server';
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const guard=await adminWriteGuard();if(guard.response)return guard.response;try{const{id}=await params;await deletePlayerData(id);await getUsers().delete(id);return NextResponse.redirect(new URL('/admin/spieler',request.url))}catch{return NextResponse.json({error:'Spieler konnte nicht gelöscht werden.'},{status:400})}}
