'use client';
import { useEffect, useState } from 'react';
type Player={rank:number;name:string;score:number;kills:number;deaths:number;assists:number;kda:number;headshots:number;accuracy:number;games:number};
export function LeaderboardTable(){const [players,setPlayers]=useState<Player[]>([]);const [loading,setLoading]=useState(true);useEffect(()=>{fetch('/api/leaderboard').then(r=>r.json()).then(d=>setPlayers(d.players||[])).catch(()=>{}).finally(()=>setLoading(false))},[]);if(loading)return <div className="leaderboard-empty"><p>Rangliste wird geladen …</p></div>;if(!players.length)return <div className="leaderboard-empty"><p>Noch keine Matches</p></div>;return <div className="leaderboard-table"><div className="leaderboard-row leaderboard-row-head"><span>#</span><span>Spieler</span><span>Spiele</span><span>KDA</span><span>Headshots</span><span>Trefferquote</span><span>Punkte</span></div>{players.map(p=><div className="leaderboard-row" key={p.name}><strong>{p.rank}</strong><span className="leaderboard-player">{p.name}<small>{p.kills} Kills · {p.deaths} Deaths · {p.assists} Assists</small></span><span>{p.games}</span><span>{p.kda}</span><span>{p.headshots}</span><span>{p.accuracy}%</span><b>{p.score.toLocaleString('de-AT')}</b></div>)}</div>}
