'use client';import{useMemo,useState}from'react';import Link from'next/link';import{Search}from'lucide-react';export function AdminPlayers({players}:{players:any[]}){const[q,setQ]=useState('');const rows=useMemo(()=>players.filter(p=>`${p.name||''} ${p.email||''}`.toLowerCase().includes(q.toLowerCase())),[players,q]);return <><div className="admin-tools player-search"><label><Search size={17}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Name oder E-Mail suchen …"/></label></div><div className="player-admin-list">{rows.map(p=><Link href={`/admin/spieler/${p.$id}`} key={p.$id}><strong>{p.name||'Ohne Namen'}</strong><span>{p.email}</span><small>Registriert: {new Date(p.$createdAt).toLocaleDateString('de-AT')} · Profil ansehen</small></Link>)}</div>{!rows.length&&<p className="admin-hint">Keine Spieler gefunden.</p>}</>}
