'use client';
import { useState } from 'react';
export function AdminPricing(){const [price,setPrice]=useState('45'),[discountedPrice,setDiscountedPrice]=useState('40'),[message,setMessage]=useState('');async function save(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/pricing',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({price:Number(price),discountedPrice:Number(discountedPrice)})});const d=await r.json().catch(()=>({}));setMessage(r.ok?'Preise gespeichert.':d.message||'Fehler beim Speichern.')}return <section className="admin-panel pricing-admin"><div className="admin-panel-head"><div><p>Website</p><h2>Preise</h2></div></div><form className="pricing-form" onSubmit={save}><label>Standardpreis (€)<input type="number" min="1" value={price} onChange={e=>setPrice(e.target.value)}/></label><label>Ermäßigter Preis (€)<input type="number" min="1" value={discountedPrice} onChange={e=>setDiscountedPrice(e.target.value)}/></label><button className="admin-primary">Speichern</button></form>{message&&<p className="admin-hint">{message}</p>}</section>}
